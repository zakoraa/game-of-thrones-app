<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".view.activity.MainActivity"
    android:fitsSystemWindows="true"
    >

    <TextView
        android:id="@+id/tv_title"
        tools:viewBindingIgnore="true"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/title"
        android:layout_marginStart="15dp"
        android:layout_marginTop="20dp"
        style="@style/title"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        />

    <androidx.cardview.widget.CardView
        android:id="@+id/cv_my_profile"
        android:layout_width="45dp"
        android:layout_height="45dp"
        android:paddingHorizontal="10dp"
        android:backgroundTint="@color/lightGrey"
        app:cardCornerRadius="200dp"
        android:layout_marginEnd="15dp"
        android:layout_marginTop="20dp"
        app:layout_constraintTop_toTopOf="@+id/tv_title"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toBottomOf="@id/tv_title"
        >
        <ImageView
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_margin="12dp"
            android:backgroundTint="@color/black"
            android:contentDescription="@string/person_icon"
            android:src="@drawable/person"/>
    </androidx.cardview.widget.CardView>

    <Spinner
        android:id="@+id/spn_choose_house"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toBottomOf="@id/tv_title"
        app:layout_constraintStart_toStartOf="@id/tv_title"
        android:layout_marginTop="8dp" />

    <Spinner
        android:id="@+id/spn_choose_layout"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintStart_toEndOf="@id/spn_choose_house"
        app:layout_constraintTop_toTopOf="@id/spn_choose_house"
        app:layout_constraintBottom_toBottomOf="@id/spn_choose_house"
        android:layout_marginTop="8dp" />

   <androidx.recyclerview.widget.RecyclerView
       android:id="@+id/rv_character_list"
       android:layout_width="match_parent"
       android:layout_height="0dp"
       android:layout_marginTop="45dp"
       android:layout_marginHorizontal="15dp"
       app:layout_constraintStart_toStartOf="@id/tv_title"
       app:layout_constraintTop_toBottomOf="@id/tv_title"
       app:layout_constraintBottom_toBottomOf="parent"
       />


</androidx.constraintlayout.widget.ConstraintLayout>